#include <stdio.h> // library header files
#include <math.h> // header file

int main()
{
    int a = 5;
    float m = pow(a, 2);
    printf("The square of number is %f\n", m);
}
//  char < int < float < double (Automatic or implicit type conversion)