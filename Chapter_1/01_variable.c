#include <stdio.h>

int main()
{
    int a;     // variable declaration
    a = 6;     // variable intialization(It can be done in the same line and it is given a constant value)
    a = a + 4; // variable modification
    int _b;    // First character must be a letter or underscore
    int c_d;   // No commas or blanks allowed in variable names
    int d;     // No special symbol other than (_) allowed in variable names
    int e;
    int E; // Variable names are case_sensitive

    printf("The output of this program is %d\n", a);
    return 0;
}